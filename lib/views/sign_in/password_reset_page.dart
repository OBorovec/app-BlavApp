import 'package:flutter/material.dart';

class PasswordResrtPage extends StatelessWidget {
  const PasswordResrtPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
