import 'package:flutter/material.dart';

class DegustationOverview extends StatelessWidget {
  const DegustationOverview({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
